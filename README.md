# sushi-api

Minimal FastAPI service scaffold.

## Setup

```bash
uv sync
```

## Run

```bash
uv run uvicorn app.main:app --reload
```

API will be available at `http://127.0.0.1:8000`.

## Endpoints

- `GET /health`